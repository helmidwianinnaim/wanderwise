[phases.setup]
nixPkgs = ["php", "php81Packages.composer", "nodejs"]

[phases.install]
cmds = ["composer install", "npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "php artisan serve --host 0.0.0.0 --port $PORT"
